tree:
  makeCmd: "make"
  makeThreads: 10
  portsDir: "/usr/ports"

db:
  url: "postgres://postgres:postgres@localhost/portscout?sslmode=disable"
  pageSize: 10

crawler:
  queueSize: 10

crawlLimiter:
  maxReqsCount: 5
  maxReqsWindowMs: 1000
